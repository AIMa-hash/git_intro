print("manish kumar singh")
print("ho, how are you?")
name = "mnaish"
if len(name):
    print("non-zero length name.")
else:
    pass
#use "git rm --cached <file>..." to unstage
#use "git restore <file>..." to discard changes in working directory
#git commit -a <file>
#use "git restore --staged <file>..." to unstages
print("not sure why it did not change")
print("not sure if this second git branch is going to be shown in the git log or not. let's see.")
print("last statement")
"""Previous HEAD position was 16e0831 modified git_intro.py in branch manish
Switched to branch 'manish'"""
